<ul class="list-group">
  <% @expenses.each do |expense| %>
    <li class="list-group-item" onclick="location.href='<%= expense_path(expense.id) %>';">
      <div class="row">
      <div class="col-2">
        <%= expense.date.strftime("%Y/%m/%d") %>
      </div>
        <div class="col-5">
          <%= expense.name %>
        </div>
      <div id="amount" class="col-2" data-a-sign="" data-a-dec="," data-a-sep=".">
        ¥<%= expense.amount%>
      </div>
      <div class="col-3">
        <%= User.find(expense.payer_id_1).name %>
      </div>
      </div>
    </li>
  <% end %>
</ul>